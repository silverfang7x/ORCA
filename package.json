{
  "name": "orca",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "packages/shared"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=backend\" \"npm run dev --workspace=frontend --if-present\"",
    "build:shared": "npm --workspace=packages/shared run build",
    "build:backend": "npm run build:shared && npm --workspace=backend run build",
    "build:frontend": "npm run build:shared && npm --workspace=frontend run build",
    "build": "npm run build:backend && npm run build:frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
